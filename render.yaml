services:
  - type: web
    name: football-prediction-bot
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot1.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
